<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Hello</h1>
    <p>Votre email est <%= user.email %></p>
    <% if (user.firstname) { %>
      <p>Votre nom est <%= user.firstname %> <%= user.lastname %></p>
    <% } %>
  </body>
</html>
